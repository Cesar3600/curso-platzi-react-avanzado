
CREANDO PRIMER COMPONENTE CATEGORY

instalando styled-components:
npm i -S styled-components


crear archivo App.js dentro de carpeta src

////////////////////////////////////////////////////////////////////////////
App.js:
---------------------------------------------------------------------------
import React from 'react'

export const App = () => {
    return(
        <h2>hello world! klasse nummer sieben</h2>
    ) 

}

///////////////////////////////////////////////////





crear la carpeta category dentro de carpeta src
dentro crear el archivo index.js

cons el siguiente contenido:

////////////////////////////////////////////////////////////////////////////
index.js
----------------------------------------------------------------------------
import React from 'react'
import { Anchor, Imagen } from './styles'

const IMG_DEFAULT = 'https://imgur.com/dJa0Hpl.jpg'

export const Category = ({ cover = IMG_DEFAULT, path, emoji }) => {
  return(
    <Anchor href={path}>
        <Imagen src={cover}/>
        {emoji}
    </Anchor>
  )
}
////////////////////////////////////////////////////////////////////////////


agregar estilos utilizando styled-components,
importar Styled desde styled components y exportar cada variable 
/////////////////////////////////////////////////////////////////////////////
styled.js
----------------------------------------------------------------------------
import Styled from 'styled-components'

export const Anchor = Styled.a`
  display: flex;
  text-align: center;
  flex-direction: column;
  text-decoration: none;
  width: 75px;
`
export const Imagen = Styled.img`
  border-radius: 50%;
  box-shadow: 0 10px 14px rgba(0,0,0,.2);
  border: 1px solid #ddd;
  height: 75px;
  width: 75px;
  overflow: hidden;
  object-fit: cover;
`
////////////////////////////////////////////////////////////////////////////



en Category/index.js
////////////////////////////////////////////////////////////////////////////
index.js
----------------------------------------------------------------------------

import React from 'react'
import { Anchor, Imagen } from './styles'

const IMG_DEFAULT = 'https://imgur.com/dJa0Hpl.jpg'

export const Category = ({ cover = IMG_DEFAULT, path, emoji='?' }) => {
  return(
    <Anchor href={path}>
        <Imagen src={cover}/>
        {emoji}
    </Anchor>
  )
}

importar utilizando llaves para evaluar las variables que se exportan desde el archivo styles.js
reemplazar a por Anchor e img por Imagen



